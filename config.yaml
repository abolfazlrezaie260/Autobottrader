# Mofid EasyTrader Automation Config
app:
  url: "https://d.easytrader.ir"
  # Browser connection mode:
  # - cdp: Connect to existing browser running with --remote-debugging-port=9222
  # - persistent: Launch Chrome with dedicated local profile
  connection_mode: "cdp"
  user_data_dir: "./browser_profile"
  cdp_endpoint: "http://127.0.0.1:9222"
  headless: false

order:
  symbol: "داروند"          # Target stock symbol (e.g. داروند, لبن, فولاد, شپنا)
  side: "buy"              # buy or sell
  quantity: 1000           # Order volume
  price: 0                 # 0 for automatic ceiling / max price
  use_ceiling_price: true  # Auto click or populate ceiling price

schedule:
  enabled: true
  # Target execution time (HH:MM:SS.mmm)
  target_time: "08:44:59.850"
  max_attempts: 5
  interval_ms: 250

anti_detection:
  random_delay_min_ms: 60
  random_delay_max_ms: 180
  human_mouse_movement: true

storage:
  save_stocks_data: true   # Auto-save full data of each opened stock to a text file
  output_dir: "./stocks_data"
